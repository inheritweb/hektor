name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

env:
  CI: true
  NEXT_TELEMETRY_DISABLED: 1
  PUBLIC_BASE_URL: http://localhost:3000
  SUPABASE_URL: http://127.0.0.1:54321
  SUPABASE_ANON_KEY: local-ci-placeholder
  SUPABASE_SERVICE_ROLE_KEY: local-ci-placeholder
  HEKTOR_ADMIN_EMAILS: admin@example.com
  SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID: local-ci.apps.googleusercontent.com
  SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET: local-ci-placeholder

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: corepack enable
      - run: yarn install --immutable
      - run: cp .env.example .env
      - run: yarn check
      - run: yarn test
      - run: yarn build
      - run: yarn storybook:build

  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: corepack enable
      - run: yarn install --immutable
      - name: Start local Supabase
        run: yarn supabase:start
      - name: Run database integration tests
        run: yarn test:integration

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: corepack enable
      - run: yarn install --immutable
      - run: cp .env.example .env
      - name: Start local Supabase
        run: yarn supabase:start >/dev/null
      - run: yarn workspace @hektor/web playwright install --with-deps chromium
      - run: yarn test:e2e
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: apps/web/playwright-report/
          retention-days: 14
