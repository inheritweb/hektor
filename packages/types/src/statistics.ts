export interface PlatformStatistics {
  organisationCount: number;
  userCount: number;
}

export interface OrganisationStatistics {
  cohortCount: number;
  groupCount: number;
  userCount: number;
}
